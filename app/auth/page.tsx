import {AuthForm, Container} from "@/shared/components";

export default function Auth() {
    return (
        <main>
            <Container>
                <AuthForm/>
            </Container>
        </main>
    );
}