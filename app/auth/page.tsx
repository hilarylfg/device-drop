import {Container} from "@/shared/components";

export default function Auth() {
    return (
        <main>
            <Container>
                Auth
            </Container>
        </main>
    );
}