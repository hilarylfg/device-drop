import {Container} from "@/shared/components";

export default function Cart() {
    return (
        <main>
            <Container>
                Cart
            </Container>
        </main>
    );
}